
  <div class="card{% if params.clickable %} card--clickable{% endif %}" data-test="{{ params.id }}">
    <h2 class="govuk-heading-m card__heading">
      {%- if params.href %}
        <a class="govuk-link card__link" href="{{ params.href }}">{{ params.heading | safe }}</a>
      {%- else %}
        {{ params.heading }}
      {%- endif %}
    </h2>

    {%- if params.description %}
      <p class="govuk-body card__description">{{ params.description | safe }}</p>
    {%- endif %}
    {%- if params.startTime %}
      <p class="govuk-body card__description">{{ params.startTime | safe }}</p>
    {%- endif %}
    {%- if params.endTime %}
      <p class="govuk-body card__description">{{ params.endTime | safe }}</p>
    {%- endif %}
    {%- if params.location %}
      <p class="govuk-body card__description">{{ params.location | safe }}</p>
    {%- endif %}
  </div>

